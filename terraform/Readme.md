# run it
```
make apply do_token=$(do_token) circleci_token=$(circleci_token) do_token_get_kubeconf=$(do_token_get_kubeconf)
```

# setup circleci terraform provider
```
make tf-provider-circleci
```
