image:
  name: {{ .Environment.Values.matrix.synapse.repo }}
  tag: {{ .Environment.Values.matrix.synapse.tag }}
postgres:
  user: {{ .Environment.Values.synapse.postgres.user }}
  password: {{ .Environment.Values.synapse.postgres.password }}
  storageSize: 10Gi
signingKeyB64: {{ .Environment.Values.synapse.signingKeyB64 }}
