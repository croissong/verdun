{{- template "common.service" (list . "mychart.mail.service") -}}
{{- define "mychart.mail.service" -}}

spec:
  ports:
  - name: riot
    protocol: TCP
    port: 80
    targetPort: riot

{{- end -}}
