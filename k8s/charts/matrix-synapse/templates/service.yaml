{{- template "common.service" (list . "mychart.mail.service") -}}
{{- define "mychart.mail.service" -}}

spec:
  ports:
    - name: http
      protocol: TCP
      port: 8008
      targetPort: http
    - name: metrics
      port: 9000
      targetPort: metrics

{{- end -}}
