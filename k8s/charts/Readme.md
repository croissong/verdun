# traefik
```
rm -fr traefik && curl https://codeload.github.com/containous/traefik-helm-chart/tar.gz/master | tar -xz && mv traefik-helm-chart-master traefik
```

# tekton
```
curl https://codeload.github.com/eddycharly/helm-prow-tekton/tar.gz/master | \
  tar -xz --strip=2 helm-prow-tekton-master/helm/tekton
```

# postgresql-operator
```
curl https://codeload.github.com/zalando/postgres-operator/tar.gz/master | \
  tar -xz --strip=2 postgres-operator-master/charts/postgres-operator
```

